class Item {
    constructor(code, description, price, isExtra) {
        this.code = code;
        this.description = description;
        this.price = price;
        this.isExtra = isExtra;
    }
}

export {Item}